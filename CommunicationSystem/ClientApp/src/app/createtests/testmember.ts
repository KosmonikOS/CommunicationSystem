export class TestMember {
  constructor(
    public testId: number,
    public userId: number,
    public name: string,
    public grade: string,
    public isSelected: boolean,
    public id: number = 0,
  ) { };
}
